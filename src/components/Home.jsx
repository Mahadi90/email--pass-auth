import React from 'react';
import { Outlet } from 'react-router-dom';
import Hedaer from './Hedaer';

const Home = () => {
    return (
        <div>
         <h2>Home</h2>
        </div>
    );
};

export default Home;